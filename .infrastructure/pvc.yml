apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-data
  namespace: mateapp
spec:
  volumeName: pv-data
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
